services:
  - type: web
    name: my-ai-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app" --host 0.0.0.0 --port 10000"
    plan: free
